// Transforma esta declaración de función a una función de expresión

function example (a, b, c){ //función declarativa
    return a+b+c
    }

let example1 = function(a, b, c){ //función de expresión
    return a+b+c 
}